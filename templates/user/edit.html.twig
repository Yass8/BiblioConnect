{% extends 'base_admin.html.twig' %}

{% block title %}Edit User Roles{% endblock %}

{% block body %}
    <h1>Edit User</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('Update role') }}</button>
        {{ form_end(form) }}

    <a href="{{ path('app_user_index') }}">back to list</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
